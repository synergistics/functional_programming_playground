(load "lib/util/require")
(require '(
           "lib/streams/head.scm"
           "lib/streams/tail.scm"
           "lib/streams/streams.scm"))

(define (set)
  )

(define (where))


(define N
  (stream 1))

